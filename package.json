{
  "name": "xpressprofx",
  "version": "1.0.0",
  "description": "XpressPro FX - Forex Trading & Investment Broker Platform",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "install:all": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
    "build": "node build.js",
    "start": "node artifacts/api-server/dist/index.mjs",
    "dev:api": "cd artifacts/api-server && PORT=8080 pnpm run dev",
    "dev:web": "cd artifacts/nextrade && PORT=3000 BASE_PATH=/ pnpm run dev",
    "dev:admin": "cd artifacts/admin-portal && PORT=3001 BASE_PATH=/xpadmin/ pnpm run dev"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
