[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["node build.js"]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
